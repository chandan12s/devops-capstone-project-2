apiVersion: v1
kind: ConfigMap
metadata:
  name: analytics-config
  namespace: analytics
  labels:
    app: analytics-platform
    environment: dev
data:
  ENVIRONMENT: "dev"
  PROCESS_INTERVAL: "10"
  LOG_LEVEL: "INFO"
  APP_VERSION: "1.0.0"
